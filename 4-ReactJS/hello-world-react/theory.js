/**
 * - ¿Que es una librería?
 *      * Un codigo encapsulado (un .js) que nos proporciona una funcionalidad
 *             EJ: Una serie de utilidades avanzadas de matematicas
 *                 que tendríamos que crear nosotros mismos sino.
 * - ¿Que es React?
 *      - Es una librería que nos ayuda a crear IU's (Interfaces de Usuario)
 *      - Si lo juntamos con otras librerías podemos crear SPA's
 * - ¿Que es ReactDOM?
 *      - Para crear una SPA, necesito mucho conocimiento de como funciona el DOM
 *      - ReactDOM -> librería que facilita la gestión del DOM
 * 
 * - ¿Que cosas tiene ReactJS?
 *      - Componentes: La página está dividida en componentes 
 *                     que vamos generando y construyendo con react
 * 
 * - ¿Que es JSX? 
 * 
 *      - Es un nuevo formato de archivo, que mezcla JavaScript con HTML
 *      - Nos permite crear componentes de React de una manera
 *        mas visual, sobre todo su HTML
 *      ej: const name = 'Alex';
 *          const element = <h1>Hello, world! {name}</h1>; // es JSX
 *          
 *      Cuando en la parte de HTML, dentro de JSX, 
 *      abro {}, lo que hay dentro es un contexto JS, es decir,
 *      puedo acceder a variables y metodos de una clase y el código
 *      que se escribe es JS
 * 
 * 
 * 
 * 
 * 
 */